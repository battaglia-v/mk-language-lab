#!/usr/bin/expect -f
set timeout 300
spawn bubblewrap build --skipPwaValidation
expect {
  -re "\\(Y/n\\)" {
    send "y\r"
    exp_continue
  }
  -re "Password for the Key Store:" {
    send "MKLanguage2025!\r"
  }
}
expect -re "Password for the Key:"
send "MKLanguage2025!\r"
expect eof
