---
phase: 12.1-ux-fixes
plan: 02
type: execute
---

<objective>
Add English translations to A2 vocabulary using the translation script from Plan 01.

Purpose: Complete bilingual content coverage for A2 level.
Output: A2 curriculum with English translations for all vocabulary items.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12.1-ux-fixes/12.1-01-SUMMARY.md

# Key files:
@scripts/curriculum/translate-vocabulary.ts - Translation script (from Plan 01)
@data/curriculum/structured/a2-lozje.json - A2 vocabulary (9,905 items)

**Scope:**
- A2 has 9,905 vocabulary items to translate
- Reuse translation script from Plan 01
- Grammar note translations (46 notes with examples)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate A2 vocabulary to English</name>
  <files>data/curriculum/structured/a2-lozje.json</files>
  <action>
Run translation script for A2 level:

```bash
ANTHROPIC_API_KEY=xxx npx tsx scripts/curriculum/translate-vocabulary.ts --level a2
```

Script should:
- Load a2-lozje.json
- Extract unique Macedonian words
- Batch translate via Claude API (50 words per batch)
- Update vocabulary items with `translation` field
- Save updated JSON

Handle larger volume (9,905 items vs 5,201 in A1) - may need progress logging.
  </action>
  <verify>
npx tsx -e "
const data = require('./data/curriculum/structured/a2-lozje.json');
let withTranslation = 0, total = 0;
for (const ch of data.chapters) {
  for (const v of ch.vocabularyItems) {
    total++;
    if (v.translation) withTranslation++;
  }
}
console.log('A2 translations:', withTranslation, '/', total);
process.exit(withTranslation < total * 0.9 ? 1 : 0);
"
  </verify>
  <done>90%+ of A2 vocabulary items have English translations</done>
</task>

<task type="auto">
  <name>Task 2: Translate grammar note examples</name>
  <files>data/curriculum/structured/a1-teskoto.json, data/curriculum/structured/a2-lozje.json</files>
  <action>
Add translation capability for grammar examples:

1. Update translate-vocabulary.ts or create translate-grammar.ts:
   - Extract example sentences from grammarNotes
   - Translate Macedonian sentences to English
   - Add `translatedExamples` array to each grammar note

2. Run for both A1 and A2:
   ```bash
   npx tsx scripts/curriculum/translate-grammar.ts --level a1
   npx tsx scripts/curriculum/translate-grammar.ts --level a2
   ```

Grammar notes structure update:
```typescript
{
  title: "Сегашно време (презент)",
  content: "...",
  examples: ["Јас сум студент.", ...],
  translatedExamples: ["I am a student.", ...]  // NEW
}
```
  </action>
  <verify>
npx tsx -e "
const a1 = require('./data/curriculum/structured/a1-teskoto.json');
const a2 = require('./data/curriculum/structured/a2-lozje.json');
let translated = 0, total = 0;
for (const ch of [...a1.chapters, ...a2.chapters]) {
  for (const note of ch.grammarNotes) {
    total++;
    if (note.translatedExamples?.length > 0) translated++;
  }
}
console.log('Grammar notes with translations:', translated, '/', total);
"
  </verify>
  <done>Grammar notes have translated examples for both A1 and A2</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] A2 vocabulary has 90%+ English translations
- [ ] Grammar notes have translated examples
- [ ] JSON files are valid (can be loaded without errors)
- [ ] No duplicate or garbled translations
</verification>

<success_criteria>
- All tasks completed
- A2 vocabulary coverage matches A1
- Grammar examples are properly translated
- Phase 12.1 complete
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-ux-fixes/12.1-02-SUMMARY.md` with:
- A2 translation statistics
- Grammar translation statistics
- Total API costs for phase
- Ready for Phase 13 (B1 Content Bootstrap)
</output>
