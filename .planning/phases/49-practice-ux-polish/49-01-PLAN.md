---
phase: 49-practice-ux-polish
plan: 01
type: execute
---

<objective>
Polish Practice session exercise feedback with consistent animations and improved visual feedback across all practice modes.

Purpose: Create a cohesive, satisfying exercise experience with consistent animations for correct/incorrect answers, smooth transitions, and responsive choice buttons.
Output: All practice session types (PracticeSession, WordSprintSession, ClozeSession) with unified feedback patterns and smooth animations.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context:
@.planning/phases/48-learn-ux-polish/48-02-SUMMARY.md

# Key files for this plan:
@components/practice/PracticeSession.tsx
@components/practice/ClozeSession.tsx
@components/practice/word-sprint/WordSprintSession.tsx
@components/practice/word-sprint/MultipleChoiceInput.tsx
@components/practice/SessionShell.tsx
@app/globals.css

**Established patterns from Phase 48:**
- Staggered fade-in animations with 50ms delay per item
- CSS-only animations using animate-in utilities
- 48px touch targets with wrapper pattern

**Existing animations in globals.css:**
- animate-shake (incorrect answers)
- animate-feedback-correct (scale bounce)
- animate-feedback-incorrect (shake entrance)
- animate-bounce-correct (subtle scale)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify choice button animations across practice sessions</name>
  <files>components/practice/PracticeSession.tsx, components/practice/ClozeSession.tsx, components/practice/word-sprint/MultipleChoiceInput.tsx</files>
  <action>
Add staggered fade-in animation to choice buttons when they appear (consistent with VocabularySection pattern from Phase 48).

For each file:
1. Add animationDelay based on button index (index * 50ms)
2. Use animate-in fade-in-0 slide-in-from-bottom-2 classes
3. Add animationFillMode: 'backwards' to prevent flash before animation

For PracticeSession.tsx choices (around line 594):
- Apply to the button mapping in the choices.map section
- Add style={{ animationDelay: `${i * 50}ms`, animationFillMode: 'backwards' }}
- Add className animate-in fade-in-0 slide-in-from-bottom-2 duration-200

For ClozeSession.tsx (around line 190):
- Same pattern applied to choice buttons

For MultipleChoiceInput.tsx (word-sprint):
- Already has good base, add staggered animation

Avoid: Don't change the feedback state colors or shake animations - those are working well.
  </action>
  <verify>npm run type-check passes, dev server shows choice buttons animating in sequence when new question appears</verify>
  <done>Choice buttons in all 3 practice sessions have staggered fade-in animation when question changes</done>
</task>

<task type="auto">
  <name>Task 2: Add correct answer bounce animation to choice buttons</name>
  <files>components/practice/PracticeSession.tsx, components/practice/ClozeSession.tsx, components/practice/word-sprint/MultipleChoiceInput.tsx</files>
  <action>
When a correct answer is selected, add a satisfying bounce animation to the chosen button.

Use the existing animate-bounce-correct class from globals.css (scale 1 → 1.05 → 1 in 0.2s).

For each file, when feedback === 'correct' and the button is selected:
- Add 'animate-bounce-correct' class alongside the existing scale-[1.02] transform

PracticeSession.tsx (line 602):
- Change: selectedChoice === c && feedback === 'correct' && 'border-emerald-400 bg-emerald-500/20 scale-[1.02]'
- To: selectedChoice === c && feedback === 'correct' && 'border-emerald-400 bg-emerald-500/20 scale-[1.02] animate-bounce-correct'

ClozeSession.tsx (line 195):
- Same pattern

MultipleChoiceInput.tsx (line 41-42):
- Add animate-bounce-correct to the isSelected && feedback === 'correct' condition

Avoid: Don't modify the incorrect answer shake animation - it's already working well.
  </action>
  <verify>npm run type-check passes, selecting correct answer shows satisfying bounce animation</verify>
  <done>Correct answer selections have bounce animation in all practice sessions</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Staggered choice button animations and correct answer bounce across all practice modes</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:3000/en/practice
    3. Test PracticeSession:
       - Click "Vocabulary Practice" or "Lesson Review"
       - Observe choice buttons fade in with stagger (50ms apart)
       - Answer correctly - see bounce animation on correct choice
       - Answer incorrectly - see shake animation (existing)
    4. Test Word Sprint:
       - Click "Word Sprint" → select Easy difficulty
       - Observe same staggered animation pattern
       - Answer correctly - see bounce
    5. Mobile test (375px viewport):
       - Animations should be smooth, not jarring
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe any animation issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` passes without errors
- [ ] `npm run lint` passes (max 10 warnings)
- [ ] Choice buttons animate in with stagger in PracticeSession
- [ ] Choice buttons animate in with stagger in ClozeSession
- [ ] Choice buttons animate in with stagger in WordSprintSession
- [ ] Correct answers show bounce animation
- [ ] Incorrect answers still show shake animation (unchanged)
- [ ] Animations work on mobile viewport
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- Consistent animation experience across all practice session types
</success_criteria>

<output>
After completion, create `.planning/phases/49-practice-ux-polish/49-01-SUMMARY.md`
</output>
