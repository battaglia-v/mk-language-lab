---
phase: 13-b1-content-bootstrap
plan: 01
type: execute
---

<objective>
Apply stop word filtering to B1 vocabulary to reduce noise and improve quality.

Purpose: B1 currently has ~14,000 vocabulary items including proper names, prepositions, and exercise text. Filtering with the established stop word list will produce usable vocabulary.
Output: Clean B1 vocabulary with stop words removed, consistent with A1/A2 quality.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context (vocabulary filtering pattern):
@.planning/phases/10-a1-vocabulary-extraction/10-01-SUMMARY.md

# Key files:
@scripts/curriculum/vocabulary-patterns.ts
@scripts/curriculum/parse-b1-structure.ts
@data/curriculum/structured/b1-zlatovrv.json

**Tech stack available:** MACEDONIAN_STOP_WORDS (269 words), isValidVocabularyWord() helper
**Established patterns:** Stop word filtering, vocabulary validation, re-parse to apply changes
**Constraining decisions:**
- Phase 10: 269 stop words comprehensive enough
- Phase 10: 3+ character minimum for vocabulary words
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stop word filtering to B1 parser</name>
  <files>scripts/curriculum/parse-b1-structure.ts</files>
  <action>
    Import isValidVocabularyWord from vocabulary-patterns.ts.
    After extractAllVocabulary() call, filter results using isValidVocabularyWord().
    This is the same pattern used in parse-a1-structure.ts and parse-a2-structure.ts.
  </action>
  <verify>npx tsx scripts/curriculum/parse-b1-structure.ts runs without errors</verify>
  <done>B1 parser filters vocabulary with isValidVocabularyWord()</done>
</task>

<task type="auto">
  <name>Task 2: Re-parse B1 and verify vocabulary reduction</name>
  <files>data/curriculum/structured/b1-zlatovrv.json</files>
  <action>
    Run the updated B1 parser to regenerate b1-zlatovrv.json.
    Check vocabulary counts per chapter - expect significant reduction from ~1,500-2,200 to ~300-800 per lesson.
    Verify no stop words remain in output.
  </action>
  <verify>
    cat data/curriculum/structured/b1-zlatovrv.json | npx json chapters | npx json -a title vocabularyItems.length
    # Expect reduced counts
  </verify>
  <done>B1 vocabulary reduced by 50%+ with stop words removed</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] B1 parser imports and uses isValidVocabularyWord()
- [ ] b1-zlatovrv.json regenerated with filtered vocabulary
- [ ] Vocabulary counts reduced significantly per lesson
- [ ] No common stop words in vocabulary sample
</verification>

<success_criteria>
- All tasks completed
- B1 vocabulary quality improved (no stop words)
- Pattern consistent with A1/A2 filtering
</success_criteria>

<output>
After completion, create `.planning/phases/13-b1-content-bootstrap/13-01-SUMMARY.md`
</output>
