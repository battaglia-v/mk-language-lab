---
phase: 30-vocabulary-display
plan: 02
type: execute
---

<objective>
Remove redundant "Back to Dashboard" navigation links from all pages.

Purpose: Simplify navigation by removing redundant back links since the home button and logo already provide dashboard access.
Output: Cleaner page headers without duplicate "Back to Dashboard" text.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan in this phase
@.planning/phases/30-vocabulary-display/30-01-SUMMARY.md

# Files to modify
@app/[locale]/discover/page.tsx
@app/[locale]/news/NewsClient.tsx
@app/[locale]/profile/page.tsx
@app/[locale]/notifications/page.tsx
@components/shell/ShellHeader.tsx
@messages/en.json
@messages/mk.json

**User feedback:** "Remove all 'back to dashboard' buttons. The home button and Македонски at the top are sufficient."
**Pattern:** Half the left tabs say "Back to Dashboard" and half don't - inconsistent UX
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove "Back to Dashboard" links from page components</name>
  <files>
    app/[locale]/discover/page.tsx,
    app/[locale]/news/NewsClient.tsx,
    app/[locale]/profile/page.tsx,
    app/[locale]/notifications/page.tsx
  </files>
  <action>
    1. In discover/page.tsx (lines ~129-133):
       - Remove the Link with backToDashboard text and aria-label
       - Keep any other header elements that are needed

    2. In NewsClient.tsx (lines ~266-268):
       - Remove the "Back to Dashboard" Link component
       - Keep any page title or other header elements

    3. In profile/page.tsx (lines ~32-34):
       - Remove the backToDashboard Link
       - Keep the page structure intact

    4. In notifications/page.tsx (lines ~33-37):
       - Remove the backToDashboard Link and text
       - Keep notifications page layout

    For each file:
    - Verify the page still has appropriate navigation via the shell header
    - Ensure no orphaned imports remain (remove unused navT if backToDashboard was only usage)
    - Keep page titles/headers if they exist separate from the back link
  </action>
  <verify>
    npm run type-check passes
    npm run lint passes
  </verify>
  <done>
    All four page components no longer render "Back to Dashboard" links.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean up ShellHeader and i18n keys</name>
  <files>
    components/shell/ShellHeader.tsx,
    messages/en.json,
    messages/mk.json
  </files>
  <action>
    1. In ShellHeader.tsx:
       - Check line ~59 for backToDashboard usage
       - If it's rendering "Back to Dashboard" text, remove it
       - Keep the back arrow icon if used (icon-only back button is fine)
       - Keep any breadcrumb or route-based navigation logic

    2. In messages/en.json:
       - Remove the "backToDashboard" key from the navigation section
       - Keep all other navigation keys

    3. In messages/mk.json:
       - Remove the "backToDashboard" key from the navigation section
       - Keep all other navigation keys

    DO NOT remove the entire nav object or break other i18n keys. Only remove the specific backToDashboard entry.
  </action>
  <verify>
    npm run type-check passes
    npm run lint passes
    grep -r "backToDashboard" --include="*.tsx" --include="*.json" returns no matches
  </verify>
  <done>
    ShellHeader no longer references backToDashboard, i18n keys removed from both locales.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` passes
- [ ] `npm run lint` passes
- [ ] No "Back to Dashboard" text visible on any page
- [ ] Navigation still works via header icons/logo
- [ ] grep "backToDashboard" returns no matches in src
</verification>

<success_criteria>

- All "Back to Dashboard" links removed from UI
- i18n keys cleaned up
- Navigation remains functional via shell header
- No TypeScript errors
- Phase 30 complete
</success_criteria>

<output>
After completion, create `.planning/phases/30-vocabulary-display/30-02-SUMMARY.md`
</output>
