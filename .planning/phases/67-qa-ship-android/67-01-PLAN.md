---
phase: 67-qa-ship-android
plan: 01
type: execute
---

<objective>
QA validation before Play Store release.

Purpose: Ensure app is production-ready with no critical issues.
Output: Verified type-check, successful build, QA checklist complete.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/66-profile-polish/66-02-SUMMARY.md

# Mobile app:
@apps/mobile/app.config.ts
@apps/mobile/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run type-check and lint</name>
  <files>apps/mobile/</files>
  <action>
Run full type-check and lint across mobile app.

Commands:
```bash
cd apps/mobile
npm run type-check  # or npx tsc --noEmit
npx eslint . --ext .ts,.tsx
```

Fix any errors found. Document warnings (if acceptable).
  </action>
  <verify>Type-check passes with no errors</verify>
  <done>Type-check and lint pass</done>
</task>

<task type="auto">
  <name>Task 2: Test development build</name>
  <files>apps/mobile/</files>
  <action>
Verify the app starts in development mode without crashes.

```bash
cd apps/mobile
npx expo start --clear
```

Check:
- Metro bundler starts without errors
- No immediate crash errors in console
- Bundle compiles successfully

Note: Full device testing requires physical device or emulator.
Document any warnings but proceed if no blocking errors.
  </action>
  <verify>Expo starts without errors</verify>
  <done>Development build verified</done>
</task>

<task type="auto">
  <name>Task 3: Create QA checklist</name>
  <files>.planning/phases/67-qa-ship-android/QA-CHECKLIST.md</files>
  <action>
Create manual QA checklist for pre-release validation.

Checklist should cover:
- Auth: Sign in, sign out, register, forgot password
- Home: Dashboard loads, navigation works
- Learn: Level selection, lesson runner, progress tracking
- Practice: Hub loads, session runs, cards work, results show
- Reader: Story list, story viewer, tap-to-translate, save word
- Translate: Direction toggle, translate button, history, copy
- Profile: Stats display, settings open, sign out works

Mark as "To be tested on device" - this is a documentation task.
  </action>
  <verify>QA checklist created</verify>
  <done>QA checklist complete</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` passes in apps/mobile
- [ ] No blocking lint errors
- [ ] Expo development server starts
- [ ] QA checklist document created
</verification>

<success_criteria>

- Type-check and lint pass
- Development build runs without crashes
- QA checklist ready for manual testing
- Phase 67-01 QA Validation complete

</success_criteria>

<output>
After completion, create `.planning/phases/67-qa-ship-android/67-01-SUMMARY.md`
</output>
