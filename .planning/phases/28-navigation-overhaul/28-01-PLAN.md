---
phase: 28-navigation-overhaul
plan: 01
type: execute
---

<objective>
Replace the "More" catch-all menu with a direct "Resources" section and consolidate user account options into the UserMenu dropdown.

Purpose: Simplify navigation by removing the intermediary "More" page and putting tools directly accessible via a "Resources" tab, while reducing menu duplication.
Output: Updated bottom nav with "Resources" replacing "More", consolidated UserMenu with Profile/Settings/Help/About, deleted More page.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./28-01-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Key files for navigation:
@components/shell/navItems.ts
@components/shell/MobileTabNav.tsx
@components/shell/SidebarNav.tsx
@components/auth/UserMenu.tsx
@app/[locale]/more/page.tsx
@messages/en.json
@messages/mk.json

**Tech stack available:** Next.js App Router, next-intl, lucide-react icons, Tailwind CSS
**Established patterns:** Shell navigation via navItems.ts array, i18n via useTranslations hook
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Resources page and update nav items</name>
  <files>app/[locale]/resources/page.tsx, components/shell/navItems.ts</files>
  <action>
1. Create new Resources page at app/[locale]/resources/page.tsx that shows:
   - "My Saved Words" (prominent, like current More page)
   - "Language Lab" (Translator, Analyzer, Pronunciation)
   - "Macedonian News"

2. Update navItems.ts:
   - Replace the "more" item with "resources" (change id, path, and icon)
   - Use `FolderOpen` or `Wrench` icon (import from lucide-react)
   - Path: "/resources"

3. Add i18n keys in messages/en.json and messages/mk.json under "nav":
   - "resources": "Resources" / "Ресурси"
   - Keep any existing resourcesDesc keys

Pattern to follow: Look at current app/[locale]/more/page.tsx for structure, but remove Profile/Settings/Help/About (those go to UserMenu).
  </action>
  <verify>
npm run type-check passes
npm run lint passes
Navigate to /en/resources shows the new page with Saved Words, Language Lab, and News items
  </verify>
  <done>Resources page created, navItems updated to show "Resources" tab instead of "More", i18n keys added</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate UserMenu with Profile/Settings/Help/About</name>
  <files>components/auth/UserMenu.tsx, messages/en.json, messages/mk.json</files>
  <action>
1. Add these menu items to UserMenu dropdown (after Profile, before social links):
   - Settings (cog icon) → /{locale}/settings
   - Help (help-circle icon) → /{locale}/help
   - About (info icon) → /{locale}/about

2. Group them under a "More" label section for visual organization

3. Ensure proper i18n - use existing keys from "nav" namespace or add to "userMenu":
   - settings, settingsDesc
   - help, helpDesc
   - about, aboutDesc

The UserMenu already has Profile. Just add Settings, Help, About in appropriate location.

Pattern: Follow existing DropdownMenuItem patterns in UserMenu.tsx
  </action>
  <verify>
npm run type-check passes
Click user avatar → dropdown shows Profile, Settings, Help, About options
Each link navigates correctly
  </verify>
  <done>UserMenu dropdown includes Settings, Help, and About links with proper i18n</done>
</task>

<task type="auto">
  <name>Task 3: Delete More page and update test references</name>
  <files>app/[locale]/more/page.tsx</files>
  <action>
1. Delete the entire app/[locale]/more/ directory (contains only page.tsx)

2. Search for and update any test files referencing "more" paths:
   - grep for "/more" in e2e/ and tests/ directories
   - Update test selectors from "nav-more" to "nav-resources"
   - Update any URL assertions from "/more" to "/resources"

3. Search components for hardcoded "/more" links and update to "/resources"

Note: The navItems.ts change in Task 1 handles the navigation item. This task handles cleanup of the old page and any stale references.
  </action>
  <verify>
npm run type-check passes
npm run lint passes
git grep "/more" shows no stale references (except historical docs)
  </verify>
  <done>More page deleted, all test and component references updated to use /resources path</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` passes
- [ ] `npm run lint` passes
- [ ] Bottom nav shows "Resources" instead of "More" on mobile
- [ ] Sidebar nav shows "Resources" instead of "More" on desktop
- [ ] Resources page shows: My Saved Words, Language Lab, Macedonian News
- [ ] UserMenu shows: Profile, Settings, Help, About
- [ ] No references to /more path remain in active code
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Navigation simplified: "More" → "Resources" with direct tool links
- User account items consolidated in UserMenu dropdown
- No TypeScript or lint errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/28-navigation-overhaul/28-01-SUMMARY.md`
</output>
