# Expo env vars for native development
# Copy to apps/mobile/.env (the file that Expo reads) or export the same keys via EAS/CI.

# API base URL for the Expo client. Use your Next.js dev server (http://localhost:3000) locally
# or the deployed host in preview/prod.
EXPO_PUBLIC_API_BASE_URL="http://localhost:3000"

# Expo project ID (https://expo.dev/accounts/<account>/projects/<slug>)
# Needed for push notification tokens via Notifications.getExpoPushTokenAsync
# For this project: 5c712af3-a6e9-462d-8243-a119b56af569
EXPO_PUBLIC_PROJECT_ID="5c712af3-a6e9-462d-8243-a119b56af569"

# Environment label used by app.config.ts (development, preview, production)
EXPO_PUBLIC_ENV="development"
